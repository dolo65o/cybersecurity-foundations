# Windows Command Line

* `set` :- to check your path from the command line.
* `ver` :- command to determine the operating system (OS) version.
* `systeminfo` :- command to list various information about the system such as OS information, system details, processor and memory.
* `more` :- can view it page after page by pressing the space bar button. 
* `ipconfig` :- check your network information.
* `ipconfig /all` :- for more information about your network configuration. 
* ` ping target_name` :- common troubleshooting task is checking if the server can access a particular server on the Internet.
* `tracert target_name` :-traces the network route traversed to reach the target.
* `nslookup example.com` :- It looks up a host or domain and returns its IP address. 
* `netstat` :-command displays current network connections and listening ports. 
* `-h` :- displays the help page. 
* `-a` :- to check your path from the command line.
* `-b` :- to check your path from the command line.
* `set` :- to check your path from the command line.
* `set` :- to check your path from the command line.
* `set` :- to check your path from the command line.
* `set` :- to check your path from the command line.
* `set` :- to check your path from the command line.
* `set` :- to check your path from the command line.
* `set` :- to check your path from the command line.
* `set` :- to check your path from the command line.
* `set` :- to check your path from the command line.
